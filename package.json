{
  "name": "llm-api-ledger-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:plans": "node scripts/build-plans.mjs",
    "dev": "npm run build:plans && vitepress dev docs --host 127.0.0.1 --port 5173",
    "build": "npm run build:plans && vitepress build docs",
    "preview": "vitepress preview docs --host 127.0.0.1 --port 4173"
  },
  "devDependencies": {
    "vitepress": "^1.5.0",
    "js-yaml": "^4.1.0"
  }
}
